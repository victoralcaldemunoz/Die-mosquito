/* function Princess(x, y, parent) {
    this.x = x;
    this.y = y;
    this.width = 50; 
    this.height = 50; 
    this.health = 100; 
    this.sprite = document.createElement('div')

    this.createPrincess = function(){
        this.sprite.classList.add('princess')
        this.sprite.style.left = this.x + 'px'
        this.sprite.style.top = this.y + 'px'
        parent.appendChild(this.sprite)
    }

    this.takeDamage = function(damageAmount) {
        this.health -= damageAmount;
        if (this.health <= 0) {
            this.health = 0;
            // cuando la princesa se queda sin sangre has ganado. Mostrar mensaje correspondiente
        }
    };

    // Método para curar a la princesa y aumentar su nivel de vida cada x tiempo que mosquito se aleje
    this.heal = function(healAmount) {
    this.health += healAmount;
};

}

export { Princess } */





